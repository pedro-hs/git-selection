# general
README
LISCENSE
gis help

# file
gis commit|cm (gis add + git commit)
gis rm (rm when untracked, git checkout when is tracked)

# branch
gis branchd|bd (git branch -D local & remote)
gis branchld|bld (git branch -D local)
gis branchrd|brd (git branch -D remote)
gis branchc|bc (git checkout local & remote)
gis branchlc|blc (git checkout local)
gis branchrc|brc (git checkout remote)

# stash
gis stashd|sd (git stash drop)
gis stasha|sa (git stash apply)
