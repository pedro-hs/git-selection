# general
README
LISCENSE
gs help

# file
gs commit|cm (gs add + git commit)

# branch
gs branchd|bd (git branch -D local & remote)
gs branchld|bld (git branch -D local)
gs branchrd|brd (git branch -D remote)
gs branchc|bc (git checkout local & remote)
gs branchlc|blc (git checkout local)
gs branchrc|brc (git checkout remote)

# stash
gs stashd|sd (git stash drop)
gs stasha|sa (git stash apply)
